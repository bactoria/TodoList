export default {
    todoList(state) {
        return state.todoList
    },
    addModalStatus(state) {
        return state.addModalStatus
    },
    updateModalStatus(state) {
        return state.updateModalStatus
    }
}